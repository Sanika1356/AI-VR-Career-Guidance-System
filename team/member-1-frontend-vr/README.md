# Member 1: Frontend + VR

Member 1 owns the user-facing experience, including the React application, responsive design, charts, client-side API integration, and the 3D/VR career environments.

The complete delivery checklist is in [todo.md](todo.md). The implementation belongs in `client/`; this folder contains planning and ownership documentation only.

Member 1 should coordinate with Member 2 before changing API assumptions, shared types, authentication behavior, or root configuration files.
