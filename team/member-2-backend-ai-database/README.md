# Member 2: Backend + AI + Database

Member 2 owns the Node.js/Express API, PostgreSQL schema and migrations, authentication, recommendation engine, skill-gap logic, roadmap services, AI advisor integration, testing, and deployment.

The complete delivery checklist is in [todo.md](todo.md). The implementation belongs in `server/`; this folder contains planning and ownership documentation only.

Member 2 should coordinate with Member 1 before changing API response shapes, shared types, authentication behavior, or root configuration files.
